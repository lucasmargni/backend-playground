services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: vaultx
      POSTGRES_PASSWORD: vaultx
      POSTGRES_DB: vaultx
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: